<%= render "genre_search", genres: @genres %>

<h4>ようこそ、NaganoCakeへ！</h4>
このサイトは、ケーキ販売のECサイトになります。
会員でない方も商品の閲覧はできますが、
購入には会員登録が必要になります。

<% @all_ranks.each do |f| %>
    <%#= f.attachment_image_tag(@product, :image, :fill, 100, 100, fallback: "no_image.jpg") %>
    <%= all_ranks.attachment_image_tag(@product, :image, :fill, 100, 100, fallback: "no_image.jpg") %>
    <%= f.name %>
<% end %>